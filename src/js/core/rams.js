import {getData, setData, removeData, closestData} from './element.js';
import {toggle} from './toggle.js';

export const rams = {
    getData,
    setData,
    removeData,
    closestData,
    toggle,
};
